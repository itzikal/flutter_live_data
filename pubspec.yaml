name: stream_live_data
description: Flutter stream made easy
version: 0.0.19
homepage: https://github.com/itzikal/flutter_live_data

environment:
  sdk: '>=2.17.0 <3.7.1'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: